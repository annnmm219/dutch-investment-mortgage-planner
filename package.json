{
  "name": "dutch-investment-mortgage-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Static Dutch investment and mortgage planning tool",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "node scripts/browser-responsiveness-smoke.mjs && node scripts/stage7-browser-contract.mjs && node scripts/stage9-browser-contract.mjs",
    "verify:50": "node scripts/verify-50-scenarios.js",
    "verify:stage9": "node scripts/verify-stage9-input-routes.js"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "playwright": "1.55.0"
  }
}
